# Auric v1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build Auric — a Linux/GNOME system tray app that monitors Claude Code token usage, rate limits, and costs via file polling + live API pings.

**Architecture:** Single Python process. GTK main loop on main thread via GLib. File polling via `GLib.timeout_add_seconds`. API pings on background threads, results delivered via `GLib.idle_add`. MVC + dependency injection throughout.

**Tech Stack:** Python ≥3.11, PyGObject, libayatana-appindicator3, httpx, pytest, ruff, bandit, uv

---

## File Map

| File | Responsibility |
|---|---|
| `pyproject.toml` | All tool config: deps, ruff, pytest, bandit, coverage |
| `.pre-commit-config.yaml` | ruff + bandit gates |
| `auric/__init__.py` | Package marker |
| `auric/main.py` | Entry point, builds container, starts GLib loop |
| `auric/app.py` | AppController — GLib loop lifecycle, SIGTERM handler |
| `auric/container.py` | DI wiring — constructs all objects, injects deps |
| `auric/models/provider.py` | `ProviderStatus` enum, `Provider` dataclass |
| `auric/models/usage.py` | `UsageSnapshot`, `RateLimitState` dataclasses |
| `auric/models/config.py` | `AppConfig`, `ProviderConfig` dataclasses |
| `auric/config/manager.py` | Read/write `~/.config/auric/config.toml` |
| `auric/providers/base.py` | `AbstractProvider` ABC |
| `auric/providers/claude.py` | `ClaudeProvider` — file poll + API ping |
| `auric/services/detector.py` | `AutoDetector` — finds installed providers |
| `auric/services/collector.py` | `UsageCollector` — schedules GLib timers |
| `auric/services/storage.py` | `SQLiteStorage` — persists snapshots |
| `auric/views/tray.py` | `TrayIcon` — AppIndicator3 icon + click handler |
| `auric/views/menu.py` | `PopupMenu` — GTK menu built from Provider list |
| `tests/unit/test_models.py` | Model construction, field validation |
| `tests/unit/test_config_manager.py` | TOML read/write, key resolution order |
| `tests/unit/test_detector.py` | Detection logic with fake filesystem |
| `tests/unit/test_claude_provider.py` | File parsing + API header extraction |
| `tests/unit/test_storage.py` | SQLite read/write |
| `tests/unit/test_collector.py` | Orchestration with stub providers |
| `tests/integration/test_file_poller.py` | Reads real fixture, full parse |
| `tests/integration/test_api_ping.py` | respx mock returning ratelimit headers |
| `tests/fixtures/stats-cache.json` | Scrubbed real snapshot for integration tests |

---

## Task 1: Project Scaffolding

**Files:**
- Create: `pyproject.toml`
- Create: `.pre-commit-config.yaml`
- Create: `.gitignore`
- Create: `auric/__init__.py` and all `__init__.py` files
- Create: `tests/__init__.py` and subdirs

- [ ] **Step 1: Check uv is available**

```bash
which uv || curl -LsSf https://astral.sh/uv/install.sh | sh
uv --version
```

Expected: version string like `uv 0.4.x`

- [ ] **Step 2: Init git repo**

```bash
cd /var/home/shane/Documents/Projects/SessionWatcherLinux
git init
```

- [ ] **Step 3: Create pyproject.toml**

```toml
[project]
name = "auric"
version = "0.1.0"
description = "Linux/GNOME AI tool usage tracker"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "PyGObject>=3.48.0",
]

[project.scripts]
auric = "auric.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
target-version = "py311"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "S"]
ignore = ["S101", "S603", "S607"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--strict-markers -q"

[tool.coverage.run]
source = ["auric"]
omit = ["auric/views/*", "auric/main.py", "auric/app.py", "auric/container.py"]

[tool.coverage.report]
fail_under = 80

[tool.bandit]
targets = ["auric"]
severity = "medium"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "respx>=0.21",
]
```

- [ ] **Step 4: Create .pre-commit-config.yaml**

```yaml
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.4.10
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
  - repo: https://github.com/PyCQA/bandit
    rev: 1.8.3
    hooks:
      - id: bandit
        args: ["-r", "auric/", "-ll", "--quiet"]
```

- [ ] **Step 5: Create .gitignore**

```
__pycache__/
*.py[cod]
.venv/
dist/
*.egg-info/
.coverage
htmlcov/
.pytest_cache/
.ruff_cache/
~/.config/auric/
*.db
.superpowers/
```

- [ ] **Step 6: Create package skeleton**

```bash
mkdir -p auric/{models,providers,services,views,config}
mkdir -p tests/{unit,integration,fixtures}
touch auric/__init__.py
touch auric/models/__init__.py
touch auric/providers/__init__.py
touch auric/services/__init__.py
touch auric/views/__init__.py
touch auric/config/__init__.py
touch tests/__init__.py
touch tests/unit/__init__.py
touch tests/integration/__init__.py
```

- [ ] **Step 7: Install deps**

```bash
uv sync --dev
```

Expected: virtualenv created at `.venv/`, deps installed.

- [ ] **Step 8: Verify pytest runs (empty suite)**

```bash
uv run pytest
```

Expected: `no tests ran` — not an error.

- [ ] **Step 9: Commit scaffold**

```bash
git add .
git commit -m "chore: scaffold Auric project — pyproject, ruff, bandit, pytest, uv"
```

---

## Task 2: Models — First Red-Green Loop

**Files:**
- Create: `tests/unit/test_models.py`
- Create: `auric/models/usage.py`
- Create: `auric/models/provider.py`
- Create: `auric/models/config.py`

- [ ] **Step 1: Write failing tests**

Write `tests/unit/test_models.py`:

```python
from datetime import datetime, timezone

import pytest

from auric.models.usage import RateLimitState, UsageSnapshot
from auric.models.provider import Provider, ProviderStatus
from auric.models.config import AppConfig, ProviderConfig


def make_snapshot() -> UsageSnapshot:
    return UsageSnapshot(
        provider_id="claude",
        timestamp=datetime(2026, 5, 28, 10, 0, tzinfo=timezone.utc),
        model="claude-sonnet-4-6",
        input_tokens=1000,
        output_tokens=500,
        cache_read_tokens=8000,
        cache_write_tokens=200,
        cost_usd=0.0,
    )


def make_rate_limit() -> RateLimitState:
    return RateLimitState(
        provider_id="claude",
        remaining_pct=0.68,
        reset_at=datetime(2026, 5, 28, 12, 20, tzinfo=timezone.utc),
        limit_type="5hr_window",
        requests_remaining=None,
    )


class TestUsageSnapshot:
    def test_fields_stored(self):
        s = make_snapshot()
        assert s.provider_id == "claude"
        assert s.model == "claude-sonnet-4-6"
        assert s.input_tokens == 1000
        assert s.output_tokens == 500
        assert s.cache_read_tokens == 8000
        assert s.cache_write_tokens == 200
        assert s.cost_usd == 0.0

    def test_total_tokens(self):
        s = make_snapshot()
        assert s.total_tokens == 9700  # 1000+500+8000+200

    def test_immutable_by_default(self):
        s = make_snapshot()
        with pytest.raises(Exception):
            s.input_tokens = 999  # frozen dataclass


class TestRateLimitState:
    def test_fields_stored(self):
        r = make_rate_limit()
        assert r.provider_id == "claude"
        assert r.remaining_pct == pytest.approx(0.68)
        assert r.limit_type == "5hr_window"
        assert r.requests_remaining is None
        assert r.is_stale is False

    def test_stale_defaults_false(self):
        r = make_rate_limit()
        assert r.is_stale is False

    def test_remaining_pct_as_percent(self):
        r = make_rate_limit()
        assert r.remaining_pct_display == 68


class TestProvider:
    def test_active_provider(self):
        p = Provider(
            id="claude",
            display_name="Claude Max",
            status=ProviderStatus.ACTIVE,
        )
        assert p.id == "claude"
        assert p.status == ProviderStatus.ACTIVE
        assert p.rate_limit is None
        assert p.last_snapshot is None
        assert p.error_msg is None

    def test_provider_with_rate_limit(self):
        r = make_rate_limit()
        p = Provider(
            id="claude",
            display_name="Claude Max",
            status=ProviderStatus.ACTIVE,
            rate_limit=r,
        )
        assert p.rate_limit.remaining_pct == pytest.approx(0.68)

    def test_provider_statuses(self):
        for status in ProviderStatus:
            p = Provider(id="x", display_name="X", status=status)
            assert p.status == status

    def test_degraded_provider_has_error_msg(self):
        p = Provider(
            id="claude",
            display_name="Claude Max",
            status=ProviderStatus.DEGRADED,
            error_msg="Network unreachable",
        )
        assert p.error_msg == "Network unreachable"


class TestConfig:
    def test_default_provider_config(self):
        c = ProviderConfig()
        assert c.api_key == ""
        assert c.ping_interval_s == 300
        assert c.poll_interval_s == 30
        assert c.enabled is True

    def test_app_config_has_providers_dict(self):
        config = AppConfig(providers={"claude": ProviderConfig()})
        assert "claude" in config.providers
        assert config.providers["claude"].ping_interval_s == 300

    def test_app_config_empty_providers(self):
        config = AppConfig(providers={})
        assert config.providers == {}
```

- [ ] **Step 2: Run — verify FAIL**

```bash
uv run pytest tests/unit/test_models.py -v
```

Expected: `ModuleNotFoundError: No module named 'auric.models.usage'`

- [ ] **Step 3: Write auric/models/usage.py**

```python
from dataclasses import dataclass, field
from datetime import datetime


@dataclass(frozen=True)
class UsageSnapshot:
    provider_id: str
    timestamp: datetime
    model: str
    input_tokens: int
    output_tokens: int
    cache_read_tokens: int
    cache_write_tokens: int
    cost_usd: float

    @property
    def total_tokens(self) -> int:
        return self.input_tokens + self.output_tokens + self.cache_read_tokens + self.cache_write_tokens


@dataclass
class RateLimitState:
    provider_id: str
    remaining_pct: float
    reset_at: datetime
    limit_type: str
    requests_remaining: int | None
    is_stale: bool = False

    @property
    def remaining_pct_display(self) -> int:
        return round(self.remaining_pct * 100)
```

- [ ] **Step 4: Write auric/models/provider.py**

```python
from dataclasses import dataclass, field
from enum import Enum

from auric.models.usage import RateLimitState, UsageSnapshot


class ProviderStatus(Enum):
    ACTIVE = "active"
    NOT_DETECTED = "not_detected"
    RATE_LIMITED = "rate_limited"
    AUTH_ERROR = "auth_error"
    DEGRADED = "degraded"


@dataclass
class Provider:
    id: str
    display_name: str
    status: ProviderStatus
    rate_limit: RateLimitState | None = None
    last_snapshot: UsageSnapshot | None = None
    error_msg: str | None = None
```

- [ ] **Step 5: Write auric/models/config.py**

```python
from dataclasses import dataclass, field


@dataclass
class ProviderConfig:
    api_key: str = ""
    ping_interval_s: int = 300
    poll_interval_s: int = 30
    enabled: bool = True


@dataclass
class AppConfig:
    providers: dict[str, ProviderConfig] = field(default_factory=dict)
```

- [ ] **Step 6: Run — verify PASS**

```bash
uv run pytest tests/unit/test_models.py -v
```

Expected: All tests green.

- [ ] **Step 7: Ruff check**

```bash
uv run ruff check auric/models/ tests/unit/test_models.py
uv run ruff format --check auric/models/ tests/unit/test_models.py
```

Fix any issues before committing.

- [ ] **Step 8: Commit**

```bash
git add auric/models/ tests/unit/test_models.py
git commit -m "feat: add models — Provider, UsageSnapshot, RateLimitState, AppConfig"
```

---

## Task 3: Config Manager

**Files:**
- Create: `tests/unit/test_config_manager.py`
- Create: `auric/config/manager.py`

- [ ] **Step 1: Write failing tests**

Write `tests/unit/test_config_manager.py`:

```python
import tomllib
from pathlib import Path

import pytest

from auric.config.manager import ConfigManager
from auric.models.config import AppConfig, ProviderConfig


class TestConfigManager:
    def test_creates_default_config_when_absent(self, tmp_path):
        config_path = tmp_path / "config.toml"
        mgr = ConfigManager(config_path=config_path)
        config = mgr.load()
        assert isinstance(config, AppConfig)
        assert "claude" in config.providers
        assert config.providers["claude"].ping_interval_s == 300
        assert config.providers["claude"].poll_interval_s == 30
        assert config.providers["claude"].enabled is True

    def test_writes_default_config_to_disk(self, tmp_path):
        config_path = tmp_path / "config.toml"
        mgr = ConfigManager(config_path=config_path)
        mgr.load()
        assert config_path.exists()
        raw = tomllib.loads(config_path.read_text())
        assert "claude" in raw

    def test_reads_existing_config(self, tmp_path):
        config_path = tmp_path / "config.toml"
        config_path.write_text(
            "[claude]\n"
            'api_key = "sk-ant-test"\n'
            "ping_interval = 60\n"
            "poll_interval = 10\n"
            "enabled = true\n"
        )
        mgr = ConfigManager(config_path=config_path)
        config = mgr.load()
        assert config.providers["claude"].api_key == "sk-ant-test"
        assert config.providers["claude"].ping_interval_s == 60
        assert config.providers["claude"].poll_interval_s == 10

    def test_missing_keys_use_defaults(self, tmp_path):
        config_path = tmp_path / "config.toml"
        config_path.write_text("[claude]\nenabled = false\n")
        mgr = ConfigManager(config_path=config_path)
        config = mgr.load()
        assert config.providers["claude"].enabled is False
        assert config.providers["claude"].ping_interval_s == 300

    def test_creates_parent_dir_if_absent(self, tmp_path):
        config_path = tmp_path / "nested" / "dir" / "config.toml"
        mgr = ConfigManager(config_path=config_path)
        mgr.load()
        assert config_path.exists()
```

- [ ] **Step 2: Run — verify FAIL**

```bash
uv run pytest tests/unit/test_config_manager.py -v
```

Expected: `ModuleNotFoundError: No module named 'auric.config.manager'`

- [ ] **Step 3: Write auric/config/manager.py**

```python
import tomllib
import tomli_w
from pathlib import Path

from auric.models.config import AppConfig, ProviderConfig

_DEFAULTS: dict = {
    "claude": {
        "api_key": "",
        "ping_interval": 300,
        "poll_interval": 30,
        "enabled": True,
    }
}


class ConfigManager:
    def __init__(self, config_path: Path | None = None) -> None:
        self._path = config_path or Path.home() / ".config" / "auric" / "config.toml"

    def load(self) -> AppConfig:
        if not self._path.exists():
            self._path.parent.mkdir(parents=True, exist_ok=True)
            self._write_defaults()
            return self._build_config(_DEFAULTS)
        raw = tomllib.loads(self._path.read_text())
        merged = {**_DEFAULTS}
        for provider, defaults in _DEFAULTS.items():
            if provider in raw:
                merged[provider] = {**defaults, **raw[provider]}
        return self._build_config(merged)

    def _write_defaults(self) -> None:
        import tomli_w
        self._path.write_bytes(tomli_w.dumps(_DEFAULTS))

    def _build_config(self, raw: dict) -> AppConfig:
        providers = {}
        for provider_id, vals in raw.items():
            providers[provider_id] = ProviderConfig(
                api_key=vals.get("api_key", ""),
                ping_interval_s=vals.get("ping_interval", 300),
                poll_interval_s=vals.get("poll_interval", 30),
                enabled=vals.get("enabled", True),
            )
        return AppConfig(providers=providers)
```

- [ ] **Step 4: Add tomli-w dep**

`tomli_w` is needed for writing TOML. Add to pyproject.toml dependencies:

```toml
dependencies = [
    "httpx>=0.27.0",
    "PyGObject>=3.48.0",
    "tomli-w>=1.0",
]
```

Then:

```bash
uv sync --dev
```

- [ ] **Step 5: Run — verify PASS**

```bash
uv run pytest tests/unit/test_config_manager.py -v
```

Expected: All green.

- [ ] **Step 6: Ruff check**

```bash
uv run ruff check auric/config/ tests/unit/test_config_manager.py
uv run ruff format --check auric/config/ tests/unit/test_config_manager.py
```

- [ ] **Step 7: Commit**

```bash
git add auric/config/ tests/unit/test_config_manager.py pyproject.toml uv.lock
git commit -m "feat: add ConfigManager — reads/writes ~/.config/auric/config.toml"
```

---

## Task 4: AutoDetector

**Files:**
- Create: `tests/unit/test_detector.py`
- Create: `auric/services/detector.py`

- [ ] **Step 1: Write failing tests**

Write `tests/unit/test_detector.py`:

```python
import json
from pathlib import Path

import pytest

from auric.services.detector import AutoDetector
from auric.models.provider import ProviderStatus


class TestAutoDetector:
    def test_detects_claude_when_settings_exist(self, tmp_path, monkeypatch):
        claude_dir = tmp_path / ".claude"
        claude_dir.mkdir()
        (claude_dir / "settings.json").write_text(json.dumps({"theme": "dark"}))
        monkeypatch.setenv("ANTHROPIC_API_KEY", "sk-ant-test123")
        detector = AutoDetector(home_dir=tmp_path)
        result = detector.detect_claude()
        assert result.status == ProviderStatus.ACTIVE
        assert result.id == "claude"
        assert result.display_name == "Claude Max"

    def test_not_detected_when_settings_missing(self, tmp_path):
        detector = AutoDetector(home_dir=tmp_path)
        result = detector.detect_claude()
        assert result.status == ProviderStatus.NOT_DETECTED

    def test_api_key_from_env(self, tmp_path, monkeypatch):
        claude_dir = tmp_path / ".claude"
        claude_dir.mkdir()
        (claude_dir / "settings.json").write_text("{}")
        monkeypatch.setenv("ANTHROPIC_API_KEY", "sk-ant-from-env")
        detector = AutoDetector(home_dir=tmp_path)
        result = detector.detect_claude()
        assert result.status == ProviderStatus.ACTIVE

    def test_api_key_from_credentials_file(self, tmp_path, monkeypatch):
        monkeypatch.delenv("ANTHROPIC_API_KEY", raising=False)
        claude_dir = tmp_path / ".claude"
        claude_dir.mkdir()
        (claude_dir / "settings.json").write_text("{}")
        (claude_dir / ".credentials.json").write_text(
            json.dumps({"claudeAiOauthToken": "oauth-token-abc"})
        )
        detector = AutoDetector(home_dir=tmp_path)
        result = detector.detect_claude()
        assert result.status == ProviderStatus.ACTIVE

    def test_not_detected_when_no_api_key_anywhere(self, tmp_path, monkeypatch):
        monkeypatch.delenv("ANTHROPIC_API_KEY", raising=False)
        claude_dir = tmp_path / ".claude"
        claude_dir.mkdir()
        (claude_dir / "settings.json").write_text("{}")
        detector = AutoDetector(home_dir=tmp_path)
        result = detector.detect_claude()
        assert result.status == ProviderStatus.NOT_DETECTED

    def test_detect_all_returns_list(self, tmp_path, monkeypatch):
        monkeypatch.delenv("ANTHROPIC_API_KEY", raising=False)
        detector = AutoDetector(home_dir=tmp_path)
        providers = detector.detect_all()
        assert isinstance(providers, list)
        assert any(p.id == "claude" for p in providers)
```

- [ ] **Step 2: Run — verify FAIL**

```bash
uv run pytest tests/unit/test_detector.py -v
```

Expected: `ModuleNotFoundError: No module named 'auric.services.detector'`

- [ ] **Step 3: Write auric/services/detector.py**

```python
import json
import os
from pathlib import Path

from auric.models.provider import Provider, ProviderStatus


class AutoDetector:
    def __init__(self, home_dir: Path | None = None) -> None:
        self._home = home_dir or Path.home()

    def detect_all(self) -> list[Provider]:
        return [self.detect_claude()]

    def detect_claude(self) -> Provider:
        claude_dir = self._home / ".claude"
        settings = claude_dir / "settings.json"
        if not settings.exists():
            return Provider(id="claude", display_name="Claude Max", status=ProviderStatus.NOT_DETECTED)

        api_key = self._resolve_claude_key(claude_dir)
        if not api_key:
            return Provider(id="claude", display_name="Claude Max", status=ProviderStatus.NOT_DETECTED)

        return Provider(id="claude", display_name="Claude Max", status=ProviderStatus.ACTIVE)

    def _resolve_claude_key(self, claude_dir: Path) -> str:
        # Priority: env var → .credentials.json OAuth token
        env_key = os.environ.get("ANTHROPIC_API_KEY", "")
        if env_key:
            return env_key

        creds_file = claude_dir / ".credentials.json"
        if creds_file.exists():
            try:
                data = json.loads(creds_file.read_text())
                token = data.get("claudeAiOauthToken", "")
                if token:
                    return token
            except (json.JSONDecodeError, OSError):
                pass

        return ""
```

- [ ] **Step 4: Run — verify PASS**

```bash
uv run pytest tests/unit/test_detector.py -v
```

Expected: All green.

- [ ] **Step 5: Ruff check**

```bash
uv run ruff check auric/services/detector.py tests/unit/test_detector.py
uv run ruff format --check auric/services/detector.py tests/unit/test_detector.py
```

- [ ] **Step 6: Commit**

```bash
git add auric/services/detector.py tests/unit/test_detector.py
git commit -m "feat: add AutoDetector — finds Claude via settings.json + key resolution"
```

---

## Task 5: ClaudeProvider — File Poller

**Files:**
- Create: `auric/providers/base.py`
- Create: `auric/providers/claude.py` (file poll portion)
- Create: `tests/fixtures/stats-cache.json`
- Create: `tests/unit/test_claude_provider.py`
- Create: `tests/integration/test_file_poller.py`

- [ ] **Step 1: Create fixture stats-cache.json**

Write `tests/fixtures/stats-cache.json`:

```json
{
  "version": 3,
  "lastComputedDate": "2026-05-27",
  "dailyActivity": [
    {
      "date": "2026-05-27",
      "messageCount": 45,
      "sessionCount": 3,
      "toolCallCount": 120
    }
  ],
  "dailyModelTokens": [
    {
      "date": "2026-05-27",
      "tokensByModel": {
        "claude-sonnet-4-6": 84000,
        "claude-haiku-4-5-20251001": 2100
      }
    }
  ],
  "modelUsage": {
    "claude-sonnet-4-6": {
      "inputTokens": 5000,
      "outputTokens": 12000,
      "cacheReadInputTokens": 65000,
      "cacheCreationInputTokens": 2000,
      "webSearchRequests": 0,
      "costUSD": 0.0,
      "contextWindow": 0,
      "maxOutputTokens": 0
    },
    "claude-haiku-4-5-20251001": {
      "inputTokens": 800,
      "outputTokens": 500,
      "cacheReadInputTokens": 800,
      "cacheCreationInputTokens": 0,
      "webSearchRequests": 0,
      "costUSD": 0.0,
      "contextWindow": 0,
      "maxOutputTokens": 0
    }
  },
  "totalSessions": 10,
  "totalMessages": 250,
  "firstSessionDate": "2026-05-01T10:00:00.000Z"
}
```

- [ ] **Step 2: Write failing tests**

Write `tests/unit/test_claude_provider.py`:

```python
import json
from datetime import date, timezone
from pathlib import Path
from unittest.mock import MagicMock

import httpx
import pytest

from auric.providers.claude import ClaudeProvider
from auric.models.usage import UsageSnapshot


FIXTURE = Path(__file__).parent.parent / "fixtures" / "stats-cache.json"


class TestClaudeFilePoller:
    def test_poll_returns_snapshot_for_today(self, tmp_path):
        stats = json.loads(FIXTURE.read_text())
        cache_path = tmp_path / "stats-cache.json"
        cache_path.write_text(json.dumps(stats))
        provider = ClaudeProvider(
            stats_cache_path=cache_path,
            http_client=MagicMock(spec=httpx.Client),
        )
        snapshot = provider.poll()
        assert snapshot is not None
        assert snapshot.provider_id == "claude"
        assert snapshot.total_tokens == 86100  # 84000 + 2100

    def test_poll_returns_none_when_file_missing(self, tmp_path):
        provider = ClaudeProvider(
            stats_cache_path=tmp_path / "missing.json",
            http_client=MagicMock(spec=httpx.Client),
        )
        assert provider.poll() is None

    def test_poll_returns_none_when_file_malformed(self, tmp_path):
        bad = tmp_path / "stats-cache.json"
        bad.write_text("not json {{{")
        provider = ClaudeProvider(
            stats_cache_path=bad,
            http_client=MagicMock(spec=httpx.Client),
        )
        assert provider.poll() is None

    def test_poll_returns_none_when_no_entry_for_today(self, tmp_path):
        data = {"version": 3, "dailyModelTokens": [], "modelUsage": {}}
        cache_path = tmp_path / "stats-cache.json"
        cache_path.write_text(json.dumps(data))
        provider = ClaudeProvider(
            stats_cache_path=cache_path,
            http_client=MagicMock(spec=httpx.Client),
        )
        assert provider.poll() is None

    def test_snapshot_cost_zero_for_subscription(self, tmp_path):
        stats = json.loads(FIXTURE.read_text())
        cache_path = tmp_path / "stats-cache.json"
        cache_path.write_text(json.dumps(stats))
        provider = ClaudeProvider(
            stats_cache_path=cache_path,
            http_client=MagicMock(spec=httpx.Client),
        )
        snapshot = provider.poll()
        assert snapshot.cost_usd == 0.0
```

- [ ] **Step 3: Run — verify FAIL**

```bash
uv run pytest tests/unit/test_claude_provider.py -v
```

Expected: `ModuleNotFoundError: No module named 'auric.providers.claude'`

- [ ] **Step 4: Write auric/providers/base.py**

```python
from abc import ABC, abstractmethod

from auric.models.usage import RateLimitState, UsageSnapshot


class AbstractProvider(ABC):
    @abstractmethod
    def poll(self) -> UsageSnapshot | None:
        """Read local files and return today's usage snapshot, or None on failure."""

    @abstractmethod
    def ping(self) -> RateLimitState | None:
        """Make a lightweight API call and return live rate limit state, or None on failure."""
```

- [ ] **Step 5: Write auric/providers/claude.py (poll only)**

```python
import json
import logging
from datetime import date, datetime, timezone
from pathlib import Path

import httpx

from auric.models.usage import RateLimitState, UsageSnapshot
from auric.providers.base import AbstractProvider

log = logging.getLogger(__name__)


class ClaudeProvider(AbstractProvider):
    PROVIDER_ID = "claude"

    def __init__(
        self,
        stats_cache_path: Path,
        http_client: httpx.Client,
    ) -> None:
        self._cache_path = stats_cache_path
        self._http = http_client

    def poll(self) -> UsageSnapshot | None:
        try:
            raw = json.loads(self._cache_path.read_text())
        except (OSError, json.JSONDecodeError) as e:
            log.warning("Failed to read stats-cache.json: %s", e)
            return None

        today = date.today().isoformat()
        daily_tokens = raw.get("dailyModelTokens", [])
        today_entry = next((e for e in daily_tokens if e["date"] == today), None)
        if today_entry is None:
            return None

        total = sum(today_entry.get("tokensByModel", {}).values())
        model = next(iter(today_entry.get("tokensByModel", {})), "unknown")

        # model-level breakdown from cumulative modelUsage
        model_usage = raw.get("modelUsage", {}).get(model, {})

        return UsageSnapshot(
            provider_id=self.PROVIDER_ID,
            timestamp=datetime.now(tz=timezone.utc),
            model=model,
            input_tokens=model_usage.get("inputTokens", 0),
            output_tokens=model_usage.get("outputTokens", 0),
            cache_read_tokens=model_usage.get("cacheReadInputTokens", 0),
            cache_write_tokens=model_usage.get("cacheCreationInputTokens", 0),
            cost_usd=model_usage.get("costUSD", 0.0),
        )

    def ping(self) -> RateLimitState | None:
        # Implemented in Task 6
        return None
```

- [ ] **Step 6: Run — verify PASS**

```bash
uv run pytest tests/unit/test_claude_provider.py -v
```

Expected: All green. Note: `test_poll_returns_snapshot_for_today` may fail if today is not `2026-05-27`. The fixture date should be updated to today's date first:

```bash
TODAY=$(date +%Y-%m-%d)
sed -i "s/2026-05-27/$TODAY/g" tests/fixtures/stats-cache.json
```

- [ ] **Step 7: Write integration test**

Write `tests/integration/test_file_poller.py`:

```python
from pathlib import Path

import pytest

from auric.providers.claude import ClaudeProvider
from unittest.mock import MagicMock
import httpx

FIXTURE = Path(__file__).parent.parent / "fixtures" / "stats-cache.json"


def test_fixture_file_exists():
    assert FIXTURE.exists(), f"Fixture missing: {FIXTURE}"


def test_poll_parses_fixture_without_error(tmp_path):
    import shutil
    cache = tmp_path / "stats-cache.json"
    shutil.copy(FIXTURE, cache)
    # Update date to today
    import json
    from datetime import date
    data = json.loads(cache.read_text())
    today = date.today().isoformat()
    for entry in data.get("dailyModelTokens", []):
        entry["date"] = today
    for entry in data.get("dailyActivity", []):
        entry["date"] = today
    cache.write_text(json.dumps(data))

    provider = ClaudeProvider(
        stats_cache_path=cache,
        http_client=MagicMock(spec=httpx.Client),
    )
    result = provider.poll()
    assert result is not None
    assert result.provider_id == "claude"
    assert result.total_tokens > 0
```

- [ ] **Step 8: Run integration test**

```bash
uv run pytest tests/integration/test_file_poller.py -v
```

Expected: PASS.

- [ ] **Step 9: Ruff + full suite**

```bash
uv run ruff check auric/providers/ tests/
uv run ruff format --check auric/providers/ tests/
uv run pytest --cov=auric -q
```

- [ ] **Step 10: Commit**

```bash
git add auric/providers/ tests/unit/test_claude_provider.py tests/integration/test_file_poller.py tests/fixtures/stats-cache.json
git commit -m "feat: add ClaudeProvider file poller — reads stats-cache.json for daily usage"
```

---

## Task 6: ClaudeProvider — API Pinger

**Files:**
- Modify: `auric/providers/claude.py` (add `ping()`)
- Create: `tests/integration/test_api_ping.py`

- [ ] **Step 1: Write failing integration test**

Write `tests/integration/test_api_ping.py`:

```python
from datetime import datetime, timezone
from pathlib import Path
from unittest.mock import MagicMock, patch

import httpx
import pytest
import respx

from auric.providers.claude import ClaudeProvider
from auric.models.usage import RateLimitState


RESET_TIME = "2026-05-28T12:20:00Z"


@respx.mock
def test_ping_extracts_rate_limit_headers(tmp_path):
    respx.post("https://api.anthropic.com/v1/messages").mock(
        return_value=httpx.Response(
            200,
            headers={
                "anthropic-ratelimit-tokens-remaining": "68000",
                "anthropic-ratelimit-tokens-limit": "100000",
                "anthropic-ratelimit-tokens-reset": RESET_TIME,
                "anthropic-ratelimit-requests-remaining": "50",
            },
            json={"id": "msg_test", "content": [{"text": "."}]},
        )
    )

    provider = ClaudeProvider(
        stats_cache_path=tmp_path / "missing.json",
        http_client=httpx.Client(),
        api_key="sk-ant-test",
    )
    result = provider.ping()

    assert result is not None
    assert result.provider_id == "claude"
    assert result.remaining_pct == pytest.approx(0.68)
    assert result.limit_type == "5hr_window"
    assert result.requests_remaining == 50
    assert result.is_stale is False


@respx.mock
def test_ping_returns_none_on_network_error(tmp_path):
    respx.post("https://api.anthropic.com/v1/messages").mock(
        side_effect=httpx.ConnectError("connection refused")
    )
    provider = ClaudeProvider(
        stats_cache_path=tmp_path / "missing.json",
        http_client=httpx.Client(),
        api_key="sk-ant-test",
    )
    result = provider.ping()
    assert result is None


@respx.mock
def test_ping_returns_rate_limited_at_zero_on_429(tmp_path):
    respx.post("https://api.anthropic.com/v1/messages").mock(
        return_value=httpx.Response(
            429,
            headers={
                "anthropic-ratelimit-tokens-remaining": "0",
                "anthropic-ratelimit-tokens-limit": "100000",
                "anthropic-ratelimit-tokens-reset": RESET_TIME,
            },
        )
    )
    provider = ClaudeProvider(
        stats_cache_path=tmp_path / "missing.json",
        http_client=httpx.Client(),
        api_key="sk-ant-test",
    )
    result = provider.ping()
    assert result is not None
    assert result.remaining_pct == 0.0
```

- [ ] **Step 2: Run — verify FAIL**

```bash
uv run pytest tests/integration/test_api_ping.py -v
```

Expected: `TypeError: ClaudeProvider.__init__() got an unexpected keyword argument 'api_key'`

- [ ] **Step 3: Update auric/providers/claude.py to add api_key + ping()**

```python
import json
import logging
from datetime import date, datetime, timezone
from pathlib import Path

import httpx

from auric.models.usage import RateLimitState, UsageSnapshot
from auric.providers.base import AbstractProvider

log = logging.getLogger(__name__)

_PING_URL = "https://api.anthropic.com/v1/messages"
_PING_BODY = {
    "model": "claude-haiku-4-5-20251001",
    "max_tokens": 1,
    "messages": [{"role": "user", "content": "."}],
}


class ClaudeProvider(AbstractProvider):
    PROVIDER_ID = "claude"

    def __init__(
        self,
        stats_cache_path: Path,
        http_client: httpx.Client,
        api_key: str = "",
    ) -> None:
        self._cache_path = stats_cache_path
        self._http = http_client
        self._api_key = api_key

    def poll(self) -> UsageSnapshot | None:
        try:
            raw = json.loads(self._cache_path.read_text())
        except (OSError, json.JSONDecodeError) as e:
            log.warning("Failed to read stats-cache.json: %s", e)
            return None

        today = date.today().isoformat()
        daily_tokens = raw.get("dailyModelTokens", [])
        today_entry = next((e for e in daily_tokens if e["date"] == today), None)
        if today_entry is None:
            return None

        model = next(iter(today_entry.get("tokensByModel", {})), "unknown")
        model_usage = raw.get("modelUsage", {}).get(model, {})

        return UsageSnapshot(
            provider_id=self.PROVIDER_ID,
            timestamp=datetime.now(tz=timezone.utc),
            model=model,
            input_tokens=model_usage.get("inputTokens", 0),
            output_tokens=model_usage.get("outputTokens", 0),
            cache_read_tokens=model_usage.get("cacheReadInputTokens", 0),
            cache_write_tokens=model_usage.get("cacheCreationInputTokens", 0),
            cost_usd=model_usage.get("costUSD", 0.0),
        )

    def ping(self) -> RateLimitState | None:
        if not self._api_key:
            return None
        try:
            resp = self._http.post(
                _PING_URL,
                headers={
                    "x-api-key": self._api_key,
                    "anthropic-version": "2023-06-01",
                    "content-type": "application/json",
                },
                json=_PING_BODY,
                timeout=10.0,
            )
        except httpx.HTTPError as e:
            log.warning("Claude API ping failed: %s", e)
            return None

        return self._parse_rate_limit_headers(resp.headers)

    def _parse_rate_limit_headers(
        self, headers: httpx.Headers
    ) -> RateLimitState | None:
        remaining_str = headers.get("anthropic-ratelimit-tokens-remaining")
        limit_str = headers.get("anthropic-ratelimit-tokens-limit")
        reset_str = headers.get("anthropic-ratelimit-tokens-reset")
        requests_str = headers.get("anthropic-ratelimit-requests-remaining")

        if not all([remaining_str, limit_str, reset_str]):
            return None

        try:
            remaining = int(remaining_str)
            limit = int(limit_str)
            remaining_pct = remaining / limit if limit > 0 else 0.0
            reset_at = datetime.fromisoformat(reset_str.replace("Z", "+00:00"))
            requests_remaining = int(requests_str) if requests_str else None
        except (ValueError, ZeroDivisionError) as e:
            log.warning("Failed to parse rate limit headers: %s", e)
            return None

        return RateLimitState(
            provider_id=self.PROVIDER_ID,
            remaining_pct=remaining_pct,
            reset_at=reset_at,
            limit_type="5hr_window",
            requests_remaining=requests_remaining,
        )
```

- [ ] **Step 4: Run — verify PASS**

```bash
uv run pytest tests/integration/test_api_ping.py -v
```

Expected: All green.

- [ ] **Step 5: Run full suite**

```bash
uv run pytest --cov=auric -q
```

Expected: All green, coverage reported.

- [ ] **Step 6: Ruff check**

```bash
uv run ruff check auric/providers/ tests/integration/
uv run ruff format --check auric/providers/ tests/integration/
```

- [ ] **Step 7: Commit**

```bash
git add auric/providers/claude.py tests/integration/test_api_ping.py
git commit -m "feat: add ClaudeProvider API pinger — extracts live rate limits from response headers"
```

---

## Task 7: SQLiteStorage

**Files:**
- Create: `tests/unit/test_storage.py`
- Create: `auric/services/storage.py`

- [ ] **Step 1: Write failing tests**

Write `tests/unit/test_storage.py`:

```python
from datetime import datetime, timezone
from pathlib import Path

import pytest

from auric.services.storage import SQLiteStorage
from auric.models.usage import UsageSnapshot, RateLimitState


def make_snapshot(provider_id: str = "claude", total: int = 1000) -> UsageSnapshot:
    return UsageSnapshot(
        provider_id=provider_id,
        timestamp=datetime(2026, 5, 28, 10, 0, tzinfo=timezone.utc),
        model="claude-sonnet-4-6",
        input_tokens=total // 4,
        output_tokens=total // 4,
        cache_read_tokens=total // 2,
        cache_write_tokens=0,
        cost_usd=0.0,
    )


def make_rate_limit(provider_id: str = "claude", pct: float = 0.68) -> RateLimitState:
    return RateLimitState(
        provider_id=provider_id,
        remaining_pct=pct,
        reset_at=datetime(2026, 5, 28, 12, 20, tzinfo=timezone.utc),
        limit_type="5hr_window",
        requests_remaining=None,
    )


class TestSQLiteStorage:
    def test_creates_db_on_init(self, tmp_path):
        db = tmp_path / "test.db"
        storage = SQLiteStorage(db_path=db)
        assert db.exists()

    def test_save_and_retrieve_snapshot(self, tmp_path):
        storage = SQLiteStorage(db_path=tmp_path / "test.db")
        snap = make_snapshot()
        storage.save_snapshot(snap)
        results = storage.get_snapshots("claude", limit=10)
        assert len(results) == 1
        assert results[0].provider_id == "claude"
        assert results[0].total_tokens == snap.total_tokens

    def test_save_multiple_snapshots(self, tmp_path):
        storage = SQLiteStorage(db_path=tmp_path / "test.db")
        for _ in range(5):
            storage.save_snapshot(make_snapshot())
        results = storage.get_snapshots("claude", limit=10)
        assert len(results) == 5

    def test_get_snapshots_respects_limit(self, tmp_path):
        storage = SQLiteStorage(db_path=tmp_path / "test.db")
        for _ in range(10):
            storage.save_snapshot(make_snapshot())
        results = storage.get_snapshots("claude", limit=3)
        assert len(results) == 3

    def test_save_and_retrieve_rate_limit(self, tmp_path):
        storage = SQLiteStorage(db_path=tmp_path / "test.db")
        rl = make_rate_limit()
        storage.save_rate_limit(rl)
        result = storage.get_latest_rate_limit("claude")
        assert result is not None
        assert result.remaining_pct == pytest.approx(0.68)
        assert result.limit_type == "5hr_window"

    def test_get_latest_rate_limit_returns_most_recent(self, tmp_path):
        storage = SQLiteStorage(db_path=tmp_path / "test.db")
        storage.save_rate_limit(make_rate_limit(pct=0.9))
        storage.save_rate_limit(make_rate_limit(pct=0.5))
        result = storage.get_latest_rate_limit("claude")
        assert result.remaining_pct == pytest.approx(0.5)

    def test_get_latest_rate_limit_returns_none_when_empty(self, tmp_path):
        storage = SQLiteStorage(db_path=tmp_path / "test.db")
        assert storage.get_latest_rate_limit("claude") is None

    def test_creates_parent_dirs(self, tmp_path):
        db = tmp_path / "nested" / "dir" / "auric.db"
        storage = SQLiteStorage(db_path=db)
        assert db.exists()
```

- [ ] **Step 2: Run — verify FAIL**

```bash
uv run pytest tests/unit/test_storage.py -v
```

Expected: `ModuleNotFoundError: No module named 'auric.services.storage'`

- [ ] **Step 3: Write auric/services/storage.py**

```python
import sqlite3
from datetime import datetime, timezone
from pathlib import Path

from auric.models.usage import RateLimitState, UsageSnapshot

_CREATE_USAGE = """
CREATE TABLE IF NOT EXISTS usage_snapshots (
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    provider_id         TEXT NOT NULL,
    timestamp           INTEGER NOT NULL,
    model               TEXT NOT NULL,
    input_tokens        INTEGER NOT NULL,
    output_tokens       INTEGER NOT NULL,
    cache_read_tokens   INTEGER NOT NULL,
    cache_write_tokens  INTEGER NOT NULL,
    cost_usd            REAL NOT NULL
)
"""

_CREATE_RATE_LIMIT = """
CREATE TABLE IF NOT EXISTS rate_limit_snapshots (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    provider_id     TEXT NOT NULL,
    timestamp       INTEGER NOT NULL,
    remaining_pct   REAL NOT NULL,
    reset_at        INTEGER NOT NULL,
    limit_type      TEXT NOT NULL,
    requests_remaining INTEGER
)
"""

_CREATE_INDEXES = [
    "CREATE INDEX IF NOT EXISTS idx_usage_provider_ts ON usage_snapshots(provider_id, timestamp)",
    "CREATE INDEX IF NOT EXISTS idx_ratelimit_provider_ts ON rate_limit_snapshots(provider_id, timestamp)",
]


class SQLiteStorage:
    def __init__(self, db_path: Path | None = None) -> None:
        self._path = db_path or Path.home() / ".local" / "share" / "auric" / "auric.db"
        self._path.parent.mkdir(parents=True, exist_ok=True)
        self._init_db()

    def _init_db(self) -> None:
        with self._connect() as conn:
            conn.execute(_CREATE_USAGE)
            conn.execute(_CREATE_RATE_LIMIT)
            for idx in _CREATE_INDEXES:
                conn.execute(idx)

    def _connect(self) -> sqlite3.Connection:
        conn = sqlite3.connect(self._path)
        conn.row_factory = sqlite3.Row
        return conn

    def save_snapshot(self, snap: UsageSnapshot) -> None:
        with self._connect() as conn:
            conn.execute(
                "INSERT INTO usage_snapshots "
                "(provider_id, timestamp, model, input_tokens, output_tokens, "
                "cache_read_tokens, cache_write_tokens, cost_usd) "
                "VALUES (?, ?, ?, ?, ?, ?, ?, ?)",
                (
                    snap.provider_id,
                    int(snap.timestamp.timestamp()),
                    snap.model,
                    snap.input_tokens,
                    snap.output_tokens,
                    snap.cache_read_tokens,
                    snap.cache_write_tokens,
                    snap.cost_usd,
                ),
            )

    def get_snapshots(self, provider_id: str, limit: int = 100) -> list[UsageSnapshot]:
        with self._connect() as conn:
            rows = conn.execute(
                "SELECT * FROM usage_snapshots WHERE provider_id = ? "
                "ORDER BY timestamp DESC LIMIT ?",
                (provider_id, limit),
            ).fetchall()
        return [self._row_to_snapshot(r) for r in rows]

    def save_rate_limit(self, state: RateLimitState) -> None:
        with self._connect() as conn:
            conn.execute(
                "INSERT INTO rate_limit_snapshots "
                "(provider_id, timestamp, remaining_pct, reset_at, limit_type, requests_remaining) "
                "VALUES (?, ?, ?, ?, ?, ?)",
                (
                    state.provider_id,
                    int(datetime.now(tz=timezone.utc).timestamp()),
                    state.remaining_pct,
                    int(state.reset_at.timestamp()),
                    state.limit_type,
                    state.requests_remaining,
                ),
            )

    def get_latest_rate_limit(self, provider_id: str) -> RateLimitState | None:
        with self._connect() as conn:
            row = conn.execute(
                "SELECT * FROM rate_limit_snapshots WHERE provider_id = ? "
                "ORDER BY timestamp DESC LIMIT 1",
                (provider_id,),
            ).fetchone()
        if row is None:
            return None
        return self._row_to_rate_limit(row)

    def _row_to_snapshot(self, row: sqlite3.Row) -> UsageSnapshot:
        return UsageSnapshot(
            provider_id=row["provider_id"],
            timestamp=datetime.fromtimestamp(row["timestamp"], tz=timezone.utc),
            model=row["model"],
            input_tokens=row["input_tokens"],
            output_tokens=row["output_tokens"],
            cache_read_tokens=row["cache_read_tokens"],
            cache_write_tokens=row["cache_write_tokens"],
            cost_usd=row["cost_usd"],
        )

    def _row_to_rate_limit(self, row: sqlite3.Row) -> RateLimitState:
        return RateLimitState(
            provider_id=row["provider_id"],
            remaining_pct=row["remaining_pct"],
            reset_at=datetime.fromtimestamp(row["reset_at"], tz=timezone.utc),
            limit_type=row["limit_type"],
            requests_remaining=row["requests_remaining"],
        )
```

- [ ] **Step 4: Run — verify PASS**

```bash
uv run pytest tests/unit/test_storage.py -v
```

Expected: All green.

- [ ] **Step 5: Full suite + coverage**

```bash
uv run pytest --cov=auric --cov-report=term-missing -q
```

Expected: ≥80% coverage.

- [ ] **Step 6: Commit**

```bash
git add auric/services/storage.py tests/unit/test_storage.py
git commit -m "feat: add SQLiteStorage — persists usage snapshots and rate limit history"
```

---

## Task 8: UsageCollector

**Files:**
- Create: `tests/unit/test_collector.py`
- Create: `auric/services/collector.py`

- [ ] **Step 1: Write failing tests**

Write `tests/unit/test_collector.py`:

```python
from datetime import datetime, timezone
from unittest.mock import MagicMock, call

import pytest

from auric.services.collector import UsageCollector
from auric.models.provider import Provider, ProviderStatus
from auric.models.usage import RateLimitState, UsageSnapshot
from auric.providers.base import AbstractProvider
from auric.services.storage import SQLiteStorage


def make_snapshot() -> UsageSnapshot:
    return UsageSnapshot(
        provider_id="claude",
        timestamp=datetime(2026, 5, 28, 10, 0, tzinfo=timezone.utc),
        model="claude-sonnet-4-6",
        input_tokens=1000,
        output_tokens=500,
        cache_read_tokens=8000,
        cache_write_tokens=200,
        cost_usd=0.0,
    )


def make_rate_limit() -> RateLimitState:
    return RateLimitState(
        provider_id="claude",
        remaining_pct=0.68,
        reset_at=datetime(2026, 5, 28, 12, 20, tzinfo=timezone.utc),
        limit_type="5hr_window",
        requests_remaining=None,
    )


class StubProvider(AbstractProvider):
    def __init__(self, snapshot=None, rate_limit=None):
        self._snapshot = snapshot
        self._rate_limit = rate_limit
        self.poll_count = 0
        self.ping_count = 0

    def poll(self) -> UsageSnapshot | None:
        self.poll_count += 1
        return self._snapshot

    def ping(self) -> RateLimitState | None:
        self.ping_count += 1
        return self._rate_limit


class TestUsageCollector:
    def test_poll_updates_provider_snapshot(self, tmp_path):
        snap = make_snapshot()
        stub = StubProvider(snapshot=snap)
        provider = Provider(id="claude", display_name="Claude Max", status=ProviderStatus.ACTIVE)
        storage = SQLiteStorage(db_path=tmp_path / "test.db")
        collector = UsageCollector(providers={provider: stub}, storage=storage)

        collector.run_poll("claude")

        assert stub.poll_count == 1

    def test_poll_saves_snapshot_to_storage(self, tmp_path):
        snap = make_snapshot()
        stub = StubProvider(snapshot=snap)
        provider = Provider(id="claude", display_name="Claude Max", status=ProviderStatus.ACTIVE)
        storage = SQLiteStorage(db_path=tmp_path / "test.db")
        collector = UsageCollector(providers={provider: stub}, storage=storage)

        collector.run_poll("claude")

        saved = storage.get_snapshots("claude", limit=1)
        assert len(saved) == 1
        assert saved[0].total_tokens == snap.total_tokens

    def test_poll_none_does_not_save(self, tmp_path):
        stub = StubProvider(snapshot=None)
        provider = Provider(id="claude", display_name="Claude Max", status=ProviderStatus.ACTIVE)
        storage = SQLiteStorage(db_path=tmp_path / "test.db")
        collector = UsageCollector(providers={provider: stub}, storage=storage)

        collector.run_poll("claude")

        assert storage.get_snapshots("claude") == []

    def test_ping_saves_rate_limit(self, tmp_path):
        rl = make_rate_limit()
        stub = StubProvider(rate_limit=rl)
        provider = Provider(id="claude", display_name="Claude Max", status=ProviderStatus.ACTIVE)
        storage = SQLiteStorage(db_path=tmp_path / "test.db")
        collector = UsageCollector(providers={provider: stub}, storage=storage)

        collector.run_ping("claude")

        saved = storage.get_latest_rate_limit("claude")
        assert saved is not None
        assert saved.remaining_pct == pytest.approx(0.68)

    def test_ping_none_does_not_save(self, tmp_path):
        stub = StubProvider(rate_limit=None)
        provider = Provider(id="claude", display_name="Claude Max", status=ProviderStatus.ACTIVE)
        storage = SQLiteStorage(db_path=tmp_path / "test.db")
        collector = UsageCollector(providers={provider: stub}, storage=storage)

        collector.run_ping("claude")

        assert storage.get_latest_rate_limit("claude") is None

    def test_get_provider_state_returns_provider(self, tmp_path):
        stub = StubProvider()
        provider = Provider(id="claude", display_name="Claude Max", status=ProviderStatus.ACTIVE)
        storage = SQLiteStorage(db_path=tmp_path / "test.db")
        collector = UsageCollector(providers={provider: stub}, storage=storage)

        result = collector.get_provider_state("claude")
        assert result is not None
        assert result.id == "claude"

    def test_get_provider_state_unknown_returns_none(self, tmp_path):
        storage = SQLiteStorage(db_path=tmp_path / "test.db")
        collector = UsageCollector(providers={}, storage=storage)
        assert collector.get_provider_state("unknown") is None
```

- [ ] **Step 2: Run — verify FAIL**

```bash
uv run pytest tests/unit/test_collector.py -v
```

Expected: `ModuleNotFoundError: No module named 'auric.services.collector'`

- [ ] **Step 3: Write auric/services/collector.py**

```python
import logging
import threading
from typing import Callable

from auric.models.provider import Provider, ProviderStatus
from auric.models.usage import RateLimitState, UsageSnapshot
from auric.providers.base import AbstractProvider
from auric.services.storage import SQLiteStorage

log = logging.getLogger(__name__)


class UsageCollector:
    def __init__(
        self,
        providers: dict[Provider, AbstractProvider],
        storage: SQLiteStorage,
    ) -> None:
        self._providers = providers
        self._storage = storage
        self._lock = threading.Lock()
        # Index by provider id for fast lookup
        self._by_id: dict[str, tuple[Provider, AbstractProvider]] = {
            p.id: (p, impl) for p, impl in providers.items()
        }

    def run_poll(self, provider_id: str) -> None:
        entry = self._by_id.get(provider_id)
        if entry is None:
            return
        provider, impl = entry
        try:
            snapshot = impl.poll()
        except Exception as e:
            log.warning("Poll failed for %s: %s", provider_id, e)
            return
        if snapshot is None:
            return
        self._storage.save_snapshot(snapshot)
        with self._lock:
            provider.last_snapshot = snapshot

    def run_ping(self, provider_id: str) -> None:
        entry = self._by_id.get(provider_id)
        if entry is None:
            return
        provider, impl = entry
        try:
            rate_limit = impl.ping()
        except Exception as e:
            log.warning("Ping failed for %s: %s", provider_id, e)
            return
        if rate_limit is None:
            return
        self._storage.save_rate_limit(rate_limit)
        with self._lock:
            provider.rate_limit = rate_limit

    def get_provider_state(self, provider_id: str) -> Provider | None:
        entry = self._by_id.get(provider_id)
        if entry is None:
            return None
        return entry[0]

    def all_providers(self) -> list[Provider]:
        return [p for p, _ in self._by_id.values()]
```

- [ ] **Step 4: Run — verify PASS**

```bash
uv run pytest tests/unit/test_collector.py -v
```

Expected: All green.

- [ ] **Step 5: Run full suite + coverage**

```bash
uv run pytest --cov=auric --cov-report=term-missing -q
```

Expected: All green, ≥80% coverage.

- [ ] **Step 6: Bandit scan**

```bash
uv run bandit -r auric/ -ll --quiet
```

Expected: No issues at medium+ severity.

- [ ] **Step 7: Commit**

```bash
git add auric/services/collector.py tests/unit/test_collector.py
git commit -m "feat: add UsageCollector — orchestrates poll/ping, persists results, thread-safe state"
```

---

## Task 9: Views + DI Container + Entry Point

**Files:**
- Create: `auric/views/tray.py`
- Create: `auric/views/menu.py`
- Create: `auric/container.py`
- Create: `auric/app.py`
- Create: `auric/main.py`

No automated unit tests for GTK views. Manual smoke test only.

- [ ] **Step 1: Check system deps**

```bash
python3 -c "import gi; gi.require_version('AppIndicator3', '0.1'); from gi.repository import AppIndicator3; print('OK')"
```

If this fails on Fedora:

```bash
sudo dnf install libayatana-appindicator-gtk3 python3-gobject gtk3
```

- [ ] **Step 2: Write auric/views/menu.py**

```python
import gi

gi.require_version("Gtk", "3.0")
from gi.repository import Gtk

from auric.models.provider import Provider, ProviderStatus


class PopupMenu:
    def __init__(self) -> None:
        self._menu = Gtk.Menu()

    def build(self, providers: list[Provider]) -> Gtk.Menu:
        for item in self._menu.get_children():
            self._menu.remove(item)

        for provider in providers:
            self._add_provider_section(provider)

        self._menu.append(Gtk.SeparatorMenuItem())
        self._add_action("Re-detect providers", None)
        self._add_action("Settings...", None)
        self._add_action("Quit", Gtk.main_quit)
        self._menu.show_all()
        return self._menu

    def _add_provider_section(self, provider: Provider) -> None:
        if provider.status == ProviderStatus.NOT_DETECTED:
            item = Gtk.MenuItem(label=f"{provider.display_name} · not detected")
            item.set_sensitive(False)
            self._menu.append(item)
            return

        label = self._format_provider_label(provider)
        item = Gtk.MenuItem(label=label)
        item.set_sensitive(False)
        self._menu.append(item)

    def _format_provider_label(self, provider: Provider) -> str:
        parts = [provider.display_name]
        if provider.rate_limit:
            pct = provider.rate_limit.remaining_pct_display
            reset = provider.rate_limit.reset_at.strftime("%I:%M%p")
            parts.append(f"{pct}% · resets {reset}")
        if provider.last_snapshot:
            total = provider.last_snapshot.total_tokens
            cost = provider.last_snapshot.cost_usd
            parts.append(f"Today: {total:,} tokens · ${cost:.2f}")
        return " | ".join(parts)

    def _add_action(self, label: str, callback) -> None:
        item = Gtk.MenuItem(label=label)
        if callback:
            item.connect("activate", lambda _: callback())
        self._menu.append(item)
```

- [ ] **Step 3: Write auric/views/tray.py**

```python
import gi

gi.require_version("AppIndicator3", "0.1")
gi.require_version("Gtk", "3.0")
from gi.repository import AppIndicator3, Gtk

from auric.models.provider import Provider, ProviderStatus
from auric.views.menu import PopupMenu

_ICON_ACTIVE = "appointment-new"  # placeholder — replace with auric SVG
_ICON_ERROR = "dialog-warning"
_INDICATOR_ID = "auric"


class TrayIcon:
    def __init__(self) -> None:
        self._indicator = AppIndicator3.Indicator.new(
            _INDICATOR_ID,
            _ICON_ACTIVE,
            AppIndicator3.IndicatorCategory.APPLICATION_STATUS,
        )
        self._indicator.set_status(AppIndicator3.IndicatorStatus.ACTIVE)
        self._menu = PopupMenu()

    def update(self, providers: list[Provider]) -> None:
        label = self._compute_label(providers)
        self._indicator.set_label(label, "")
        self._indicator.set_menu(self._menu.build(providers))

    def _compute_label(self, providers: list[Provider]) -> str:
        active = [p for p in providers if p.status == ProviderStatus.ACTIVE]
        if not active:
            return "—"
        tightest = min(
            (p for p in active if p.rate_limit),
            key=lambda p: p.rate_limit.remaining_pct,
            default=None,
        )
        if tightest:
            return f"{tightest.rate_limit.remaining_pct_display}%"
        return "·"
```

- [ ] **Step 4: Write auric/app.py**

```python
import logging
import signal
import threading

import gi

gi.require_version("GLib", "2.0")
gi.require_version("Gtk", "3.0")
from gi.repository import GLib, Gtk

from auric.models.config import AppConfig
from auric.services.collector import UsageCollector
from auric.views.tray import TrayIcon

log = logging.getLogger(__name__)


class AppController:
    def __init__(
        self,
        config: AppConfig,
        collector: UsageCollector,
        tray: TrayIcon,
    ) -> None:
        self._config = config
        self._collector = collector
        self._tray = tray

    def start(self) -> None:
        signal.signal(signal.SIGTERM, lambda *_: self._quit())
        self._schedule_timers()
        self._initial_update()
        Gtk.main()

    def _initial_update(self) -> None:
        for provider in self._collector.all_providers():
            self._run_poll_bg(provider.id)
            self._run_ping_bg(provider.id)

    def _schedule_timers(self) -> None:
        for provider in self._collector.all_providers():
            cfg = self._config.providers.get(provider.id)
            if cfg and cfg.enabled:
                GLib.timeout_add_seconds(cfg.poll_interval_s, self._poll_tick, provider.id)
                GLib.timeout_add_seconds(cfg.ping_interval_s, self._ping_tick, provider.id)

    def _poll_tick(self, provider_id: str) -> bool:
        self._run_poll_bg(provider_id)
        return GLib.SOURCE_CONTINUE

    def _ping_tick(self, provider_id: str) -> bool:
        self._run_ping_bg(provider_id)
        return GLib.SOURCE_CONTINUE

    def _run_poll_bg(self, provider_id: str) -> None:
        def _work():
            self._collector.run_poll(provider_id)
            GLib.idle_add(self._refresh_tray)

        threading.Thread(target=_work, daemon=True).start()

    def _run_ping_bg(self, provider_id: str) -> None:
        def _work():
            self._collector.run_ping(provider_id)
            GLib.idle_add(self._refresh_tray)

        threading.Thread(target=_work, daemon=True).start()

    def _refresh_tray(self) -> bool:
        self._tray.update(self._collector.all_providers())
        return GLib.SOURCE_REMOVE

    def _quit(self) -> None:
        log.info("Shutting down Auric")
        Gtk.main_quit()
```

- [ ] **Step 5: Write auric/container.py**

```python
from pathlib import Path

import httpx

from auric.config.manager import ConfigManager
from auric.models.provider import Provider
from auric.providers.claude import ClaudeProvider
from auric.services.collector import UsageCollector
from auric.services.detector import AutoDetector
from auric.services.storage import SQLiteStorage
from auric.views.tray import TrayIcon
from auric.app import AppController


def build() -> AppController:
    config_mgr = ConfigManager()
    config = config_mgr.load()

    detector = AutoDetector()
    providers_state = detector.detect_all()

    http_client = httpx.Client()
    storage = SQLiteStorage()

    providers: dict[Provider, object] = {}
    for provider_state in providers_state:
        if provider_state.id == "claude":
            claude_cfg = config.providers.get("claude")
            api_key = claude_cfg.api_key if claude_cfg else ""
            # If no api_key in config, detector resolved it; we re-resolve here
            if not api_key:
                from auric.services.detector import AutoDetector as _D
                api_key = _D()._resolve_claude_key(Path.home() / ".claude")
            impl = ClaudeProvider(
                stats_cache_path=Path.home() / ".claude" / "stats-cache.json",
                http_client=http_client,
                api_key=api_key,
            )
            providers[provider_state] = impl

    collector = UsageCollector(providers=providers, storage=storage)
    tray = TrayIcon()

    return AppController(config=config, collector=collector, tray=tray)
```

- [ ] **Step 6: Write auric/main.py**

```python
import logging
import sys

from auric.container import build


def main() -> None:
    logging.basicConfig(
        level=logging.INFO,
        format="%(asctime)s %(name)s %(levelname)s %(message)s",
    )
    try:
        app = build()
        app.start()
    except KeyboardInterrupt:
        sys.exit(0)


if __name__ == "__main__":
    main()
```

- [ ] **Step 7: Run full test suite — must stay green**

```bash
uv run pytest --cov=auric --cov-report=term-missing -q
```

Expected: All green (views/tray/menu/app/main are excluded from coverage requirement).

- [ ] **Step 8: Ruff + bandit full pass**

```bash
uv run ruff check auric/
uv run ruff format --check auric/
uv run bandit -r auric/ -ll --quiet
```

Fix any issues.

- [ ] **Step 9: Smoke test — launch the app**

```bash
uv run auric
```

Expected: Auric tray icon appears in system tray. Click it — see provider popup. If Claude is detected, shows `Claude Max | XX% · resets HH:MMAM`. If not detected, shows greyed `Claude Max · not detected`.

- [ ] **Step 10: Commit**

```bash
git add auric/views/ auric/container.py auric/app.py auric/main.py
git commit -m "feat: add tray UI, DI container, entry point — Auric is launchable"
```

---

## Task 10: Pre-commit + Final Gates

- [ ] **Step 1: Install pre-commit**

```bash
uv add --dev pre-commit
uv run pre-commit install
```

- [ ] **Step 2: Run all hooks against full codebase**

```bash
uv run pre-commit run --all-files
```

Fix any issues surfaced by ruff or bandit.

- [ ] **Step 3: Run final full suite with coverage report**

```bash
uv run pytest --cov=auric --cov-report=term-missing -q
```

Expected: ≥80% coverage, all green.

- [ ] **Step 4: Final commit**

```bash
git add .pre-commit-config.yaml
git commit -m "chore: install pre-commit hooks — ruff + bandit gate on every commit"
```

---

## Verification Checklist

After all tasks complete:

- [ ] `uv run pytest` — all green
- [ ] `uv run ruff check auric/` — no issues
- [ ] `uv run bandit -r auric/ -ll` — no medium+ severity
- [ ] `uv run auric` — tray icon appears, popup shows Claude status
- [ ] Clicking tray shows: provider name, %, reset time, today tokens, cost
